# Relative path: the Vite dev server proxies /api -> api-gateway (see vite.config.ts),
# so the session cookie is first-party and no CORS setup is needed in development.
VITE_API_URL=/api/v1

# api-gateway address the Vite proxy forwards to
VITE_GATEWAY_URL=http://localhost:3000
